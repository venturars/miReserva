import { ReservationsClient } from './reservations-client';

describe('ReservationsClient', () => {
  it('should create an instance', () => {
    expect(new ReservationsClient()).toBeTruthy();
  });
});
