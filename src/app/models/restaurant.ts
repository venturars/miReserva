export class Restaurant {
    constructor(public name:string, public address:string, public postalcode:string, public email:string, public telephone:string, public capacity:number, public password:string, public food:string){}
}
